Step 11 ships four user-visible fixes coming out of the deploy review: (1) SPI bit-bang bug fixed in src/examples/tmp125_read.s (was using cls/mov-c on a 24-bit-extended u8, where bit 23 is always 0 -- slider only produced occasional 1s; switched to the MSB-walk-with-mask pattern from i2cwrite). (2) Device panels appear on dropdown-select rather than waiting for Run -- on_demo_select synthesises a default snapshot per declared device and preserves slider state across selections; on Run the fresh device is seeded from the current snapshot. (3) I2C Test Device panel under src/panels/i2c/test_device.rs with a poke slider for the Add1 stored byte; container now shows the card alongside TMP101. (4) SPI Test Device support (EchoDevice): src/panels/spi/echo.rs panel, DemoConfig::attach_test_spi flag, src/examples/spi_echo_ping.s demo wired in as 'SPI Echo Ping'. cargo clippy -D warnings green; pages/ rebuilt.