Ported the assembler live-demo into src/ following web-sw-cor24-x-tinyc's shape: three-pane Yew app (Editor | listing | emulator I/O), Assemble-and-Run button driving cor24_assembler -> cor24_emulator::EmulatorCore via a 16ms Interval with 10k-instr batches, keyboard->UART input, switch-S2 toggle, demo dropdown populated from sibling sw-cor24-x-assembler's example set via include_str!. Added src/{main,assembler,demos,editor,highlight,panels/*}.rs and build.rs for BUILD_SHA/HOST/TIMESTAMP; pages/ rebuilt (wasm 393 KB). README flipped from 'scaffold' to 'working demo deployed'. cargo clippy -D warnings green. 'Button Echo (MakerLisp)' deferred to a follow-up commit once dcxas's pr/makerlisp-button-echo relays into the sibling's origin/dev.