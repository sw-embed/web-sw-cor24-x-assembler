Bumped per-tick run_batch budget from 10_000 to 1_000_000 instructions. Reason: the TMP101 .lgo demo from upstream tmp101.c spins an unguarded 'int t = -1; while (t--) {}' delay loop between reads, and the old 10k/tick budget meant draining that loop took multiple wall-clock minutes -- so slider changes never showed up in the printed UART output. At 1M/tick (~60M instr/sec at 60 Hz) the demo cycles through reads several times a second. UI thread stays responsive because each tick's batch finishes in low milliseconds in browser wasm. Refreshed the emulator-side clone to pick up 8faf01f (SPI / TMP125 / multi-record tmp101.lgo reformat) -- existing looks_like_lgo sniffer handles multi-record fine. cargo clippy -D warnings green; pages/ rebuilt.