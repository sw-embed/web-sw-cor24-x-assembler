Fixed all three RTC bugs the user reported on the deploy: (1) UART input now reaches the demo by replacing the brittle div+tabindex+onkeydown capture with a real <input type=text> -- clicks reliably focus, focus survives the 60 Hz re-render cycle, and the user sees typed chars accumulate locally. (2) 'I2C RTC Read' with battery on was always 00:00:00 because nothing was ever persisted (#1 was blocking Set); resolved as a knock-on. (3) Added 'Set to system time' button on the RTC panel -- reads JS Date local time, writes via Ds1307HandleExt::set_time when a run is live, and always persists to localStorage so a subsequent battery-on Run picks it up. cargo clippy -D warnings green; pages/ rebuilt.