Battery-backed DS1307 RTC panel ships per tools/briefs/dwxas-battery-backed-rtc.md. Consumes dcemu's Ds1307Device::with_initial_registers and Ds1307HandleExt plus dcxas's i2c_ds1307_{read,set}.s. Added src/panels/i2c/rtc.rs (Ds1307Panel + battery toggle), src/battery.rs (ds1307.battery* localStorage glue with the time-of-day % 86400 contract, effective_now() folds elapsed wall-clock since last set into the boot value), I2C RTC Read + I2C RTC Set dropdown entries with DemoConfig::RTC_ONLY, and threaded the battery-toggle + DS1307 handle through main.rs's attach-path matrix. THE SYNCHRONIZATION TRAP wired in: tick-loop holds an Option<(h,m,s)> last-seen tuple; on the first non-boot crossing (last is Some AND tuple changes AND battery on) it writes the new value plus Date.now() to localStorage. One setItem per tick at most, no overwrite of persisted state by the boot read. Cargo.toml gains web-sys Storage/Window/HtmlInputElement features and js-sys/serde/serde_json. cargo clippy -D warnings green; pages/ rebuilt.