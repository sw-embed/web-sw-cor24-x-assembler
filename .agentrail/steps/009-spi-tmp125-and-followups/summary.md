Added the SPI panel framework + TMP125 device card mirroring the i2c shape: src/panels/spi/{mod,container,tmp125}.rs + main.rs attach_spi_device per run + handle.with(set_temperature) callback wired through. New 'TMP125 read (spi)' demo (src/examples/tmp125_read.s, hand-rolled 182-byte bit-bang loop) lands in the dropdown. Also rolled in two regression fixes for the deployed page: per-tick instruction budget dialed back from 1M to 100k (1M was tuned for the removed .lgo demo's huge idle loop; with all bundled demos being tight .s, 1M pegged CPU and starved slider input), and panels/uart.rs caps display to last 4 KB so flooding read-print loops don't grow an unbounded DOM text node. clippy -D warnings green; pages/ rebuilt.