Step 006 is the working TMP101 slice the user asked for and adds the first I2C demo to the dropdown. (a) src/panels/i2c/tmp101.rs gets a range-input slider (-128 to +128 °C, 0.0625 °C steps matching the 12-bit register LSB) plus an on_set_temperature callback wired through container.rs and main.rs to handle.with(|d| d.set_temperature(v)); snapshot is re-read immediately so the readout doesn't lag a tick. (b) src/assembler.rs gets looks_like_lgo(source), src/main.rs grows a two-arm dispatch so any 'L<6-hex>...' source loads via EmulatorCore::load_lgo instead of cor24-assembler. (c) src/demos.rs appends 'TMP101 read (i2c, .lgo)' include_str!'d from sibling sw-cor24-emulator/examples/i2c/tmp101/tmp101.lgo -- the demo reads the TMP101 and prints the value via UART, so dragging the slider while it runs steers the printed output. pages/ rebuilt; cargo clippy -D warnings green.