Set up pages-deploy pipeline + README live-demo link, modelled on web-sw-cor24-basic. scripts/build-pages.sh and scripts/serve.sh use port 9102 and the dist/ lock pattern; .github/workflows/pages.yml uploads ./pages and deploys via deploy-pages@v4 on push to main. pages/ seeded from a release build of the current Yew scaffold (.nojekyll + index.html + wasm bundle, 0xC0 KB). README has the live-demo URL up top; the 'Scaffold' status line is replaced with one that points at the next saga steps (assembler-tab port, then I/O panels including I2C/TMP101). cargo clippy -D warnings clean. Saga: inserted as step 003; prior step 3 (demo-extraction-port) shifted to step 4.