Three user-visible fixes addressing feedback from the deploy: (1) replaced the .lgo TMP101 entry with a readable tight-loop .s demo at src/examples/tmp101_read.s (web-local, ~290 byte program adapted from dcxas's i2c_add1_ping bit-bang library) -- editor now shows COR24 assembly when the user picks 'TMP101 read (i2c)' and the slider drives printed output snappily. (2) Wired 'I2C Add1 Ping' into the demos dropdown at the alphabetical slot per tools/briefs/dwxas-add-i2c-add1-to-dropdown.md, plus attached an Add1Device(0x50) on each Assemble & Run so the demo prints '43\\n' end-to-end (brief option 2). (3) UART panel auto-scrolls -- use_effect_with on the output length sets scroll_top=scroll_height so the latest UART characters stay visible. cargo clippy -D warnings green; pages/ rebuilt.