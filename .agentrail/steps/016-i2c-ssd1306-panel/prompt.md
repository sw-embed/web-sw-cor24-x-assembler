Implement the SSD1306 OLED panel per tools/briefs/dwxas-i2c-ssd1306-panel.md. dcemu's Ssd1306Device + dcxas's i2c_ssd1306_*.s demos are landed; refresh sibling clones first. User-direction overrides: instead of a plain canvas the panel should use an SVG image patterned on a real OLED photo with yellow top row + blue rest (the device this simulates is a 'two-color OLED' module). Locate the screenshot the user referenced and pattern-match the layout.