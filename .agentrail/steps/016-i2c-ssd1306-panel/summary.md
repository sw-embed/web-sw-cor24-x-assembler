Shipped SSD1306 OLED panel + I2C OLED demos per brief, with one user-direction override: instead of a uniform pale-green-on-black canvas, the panel models the common two-color 0.96" OLED module hardware -- pixels in rows 0..16 render yellow (#f9e2af), rest blue (#74c7ec) -- because that's a physical-display feature of the part this simulates, not a chip feature, so the colour-split lives entirely in the web layer. src/panels/i2c/ssd1306.rs uses an HTML canvas at 3x scale; PartialEq on the framebuffer Vec<u8> short-circuits re-paints when nothing changed. main.rs attaches Ssd1306Device per DemoConfig::{SSD1306_ONLY, RTC_AND_SSD1306}, polls framebuffer+display_on each tick. Dropdown: 'I2C OLED Hello' and 'I2C OLED RTC Clock' at the alphabetical slot. Cargo.toml enables web-sys CanvasRenderingContext2d + HtmlCanvasElement. cargo clippy -D warnings green; pages/ rebuilt.