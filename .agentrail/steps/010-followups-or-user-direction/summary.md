Four user-visible fixes for the deploy: (1) per-demo device attachment via DemoConfig in src/demos.rs -- 'I2C TMP101 Read' attaches only TMP101 (no test device, no SPI), 'I2C Test Device Ping' attaches only the test slave at 0x50, 'SPI TMP125 Read' attaches only TMP125; non-bus demos hide both buses; unrelated panels auto-hide so the column doesn't overflow. (2) dropdown rename + reorder: bus demos lead with 'I2C ' / 'SPI ' and the whole list is alphabetical. (3) UART panel layout: dropped flex:1 (was absorbing column space and pushing SPI panel below view) and capped inner log at 140px so the column stays dense. (4) Rename Add1 -> 'I2C Test Device' in the UI + saga plan; underlying Add1Device type unchanged pending a cross-repo brief, but the slave's role is growing beyond +1 so we use the broader name going forward.