Adaptive per-tick run-loop budget shipped. Replaced static 100k/tick with 50k-instruction chunks running back-to-back until ~8 ms of wall-clock has elapsed (via web_sys::Performance). Falls back to a single 100k chunk if Performance is unavailable. Removes the per-demo budget-tuning footgun; tight loops complete hundreds of thousands of instructions per tick while slow loops with idle delays still progress visibly, and the UI thread never blocks longer than the deadline. clippy -D warnings green; pages/ rebuilt.